# TODO: Add New User Registration Form to Login Page

## Tasks
- [ ] Add new form fields (first name, last name, username, email) to index.html, initially hidden
- [ ] Implement toggleAuthMode function in script.js to switch between login and registration modes
- [ ] Update form labels and button text based on auth mode
- [ ] Update handleAuthentication function to collect new fields for registration
- [ ] Add form validation for new registration fields
- [ ] Test the registration flow
